# AGENTS.md

## 项目目标

本项目是一个个人简历静态网站，用于展示个人履历、技术能力、项目经验和联系方式。

网站提供两个构建版本：

1. `private`：本人使用的完整版本，显示全部个人信息，并提供私有/访客预览切换按钮。
2. `visitor`：公开访问版本，不包含个人信息，也不提供切换到私有状态的能力。

网站必须提供清晰的“保存为 PDF”按钮，并针对 A4 打印和 PDF 导出优化排版。

## 技术原则

- 项目必须能够构建为静态网站，不依赖数据库或运行时后端才能展示简历。
- 简历数据、业务组件、页面入口和共享类型必须分离。
- 两个版本共享公开简历数据，私人字段通过构建环境单独注入。
- 访客构建必须在构建阶段排除私人数据，不能只依靠 CSS 或浏览器状态隐藏。
- 优先采用简单、稳定、易部署和易维护的实现。
- 页面必须兼容桌面端和移动端，并使用语义清晰、可访问性良好的 HTML 结构。

## 项目结构

网站业务代码以 `src` 为源码根目录：

```text
src/
├── app/                 # 页面路由、布局和全局样式
├── components/
│   └── resume/          # 简历业务组件
├── data/                # 允许公开的结构化简历数据
├── types/               # 共享 TypeScript 类型
├── lib/                 # 通用工具及外部能力封装
└── db/                  # 数据库相关代码（如未来需要）
```

目录约束：

- 页面入口、布局和全局样式放在 `src/app`。
- 简历相关业务组件放在 `src/components/resume`。
- 允许公开的简历内容放在 `src/data`。
- 私人信息不得写入 `src/data` 或其他源码文件。
- 跨模块导入优先使用指向 `src` 的 `@/` 路径别名。
- 不要重新创建根目录级别的 `app` 或 `db`。
- `public`、环境变量文件、构建配置、测试和基础设施文件继续放在项目根目录。

## 构建模式与环境变量

使用项目级环境变量控制构建模式：

```env
RESUME_PRIVATE=false
```

允许值：

- `true`：私有版本。
- `false`：访客版本。

未配置或值无效时，必须按访客版本处理，避免意外暴露私人信息。

真实个人字段保存在本地文件：

```text
.env.private.local
```

该文件必须被 Git 忽略。需要的字段以 `.env.example` 为准，包括：

```env
RESUME_EMAIL=
RESUME_WECHAT=
```

禁止把真实私人字段写入：

- 源码文件。
- `PUBLIC_*` 或其他会暴露给浏览器的公共环境变量。
- 访客版 HTML、JavaScript、JSON、Source Map 或其他构建产物。
- 可以提交到版本控制的配置文件。

## 本地运行与构建命令

访客版：

```bash
npm run dev:visitor
npm run build:visitor
```

私有版：

```bash
npm run dev:private
npm run build:private
```

默认命令：

```bash
npm run dev
npm run build
```

默认命令必须指向访客版，确保没有显式选择时使用安全配置。

## 私有与访客状态

私有构建的行为：

- 默认显示完整个人信息。
- 显示固定悬浮切换按钮。
- 允许在“私有”与“访客预览”之间切换。
- “访客预览”只隐藏当前界面的个人信息，用于本人检查公开效果。
- 从访客预览切回私有状态后，可以重新显示完整信息。

访客构建的行为：

- 不渲染个人信息区域。
- 不显示悬浮切换按钮。
- 不提供切换到私有状态的入口。
- 构建产物中不得存在真实个人字段。

必须区分以下两个概念：

- 私有构建中的“访客预览”是界面预览，不是安全边界。
- `build:visitor` 生成的访客构建产物才是可以公开部署的版本。

`RESUME_PRIVATE` 是构建配置，不是用户认证系统。如果未来需要在同一线上地址安全访问私有版本，必须增加真正的服务端认证；这不属于当前静态网站的默认范围。

## 简历数据模型

公开简历数据应使用 TypeScript 结构化维护，并集中放在 `src/data`。数据可以覆盖：

- 姓名与职业定位。
- 个人简介。
- 技术能力。
- 教育、工作和实习经历。
- 项目经历。
- 荣誉、证书或其他公开信息。

共享数据类型放在 `src/types`，不要在多个组件中重复定义。

个人联系方式和其他私人字段必须由私有环境变量提供，不能混入公开简历数据。可见性过滤应集中在页面入口或统一的数据边界中实现，不得散落在多个视觉组件中。

## PDF 导出

网站必须提供明确的“保存为 PDF”按钮。基础实现使用：

```js
window.print()
```

同时必须提供专门的打印样式：

- 使用 `@media print` 和 `@page`。
- 默认纸张为 A4，并设置合理页边距。
- 隐藏导航、按钮、悬浮切换器和其他纯交互元素。
- 避免标题与正文、单条经历或项目卡片被不合理地跨页切断。
- 打印时使用适合白纸阅读的颜色、字号和间距。
- 链接在 PDF 中应保持可识别和可点击。
- 导出的内容必须与当前界面状态一致。
- 私有状态可以导出完整内容；访客状态和访客预览只能导出公开内容。

如浏览器打印无法稳定满足排版要求，可以在构建阶段增加无头浏览器生成的预制 PDF，但网页打印按钮和打印样式仍需保留。

## 视觉与交互要求

- 风格应专业、简洁、有个人辨识度，避免过度模板化。
- 首屏应清楚呈现姓名、定位、简介和关键操作。
- 项目经历应突出个人贡献、使用技术和可量化成果。
- 使用项目头像时保持合理裁切，不拉伸、不变形。
- PDF 按钮在桌面端和移动端都应容易发现和操作。
- 私有状态悬浮按钮不得遮挡正文或打印内容。
- 动效应克制，并尊重 `prefers-reduced-motion`。

## 响应式要求

- 小屏幕下主要标题、个人介绍和板块引导文案应相对居中，保持自然的视觉重心与阅读节奏。

## 开发与验证

普通页面修改后至少执行：

```bash
npm run build:visitor
```

涉及以下内容时，必须同时验证访客版和私有版：

- 环境变量。
- 私人信息。
- 构建模式。
- 私有/访客切换逻辑。
- PDF 内容可见性。

对应命令：

```bash
npm run build:visitor
npm run build:private
```

验证清单：

1. 访客版不显示个人信息和悬浮切换按钮。
2. 访客构建产物中不存在真实手机号、邮箱或其他私人字段。
3. 私有版显示完整个人信息和悬浮切换按钮。
4. 私有版能够切换到访客预览，并能恢复私有状态。
5. 两种模式均可成功构建。
6. “保存为 PDF”按钮可用，打印内容符合当前状态。
7. 打印预览为 A4 布局，没有明显溢出、截断或意外空白页。
8. 页面在常见桌面和移动尺寸下可正常阅读。

优先为构建模式和可见性边界添加自动化测试，防止私人字段意外进入访客版。

## 修改约束

- 保持改动聚焦，不覆盖与当前任务无关的用户文件。
- 不提交真实密钥、`.env.private.local` 或不应公开的简历数据。
- 不得为了隐藏信息而只使用 CSS；必须从访客数据和构建产物中排除。
- 新增依赖前确认其必要性，避免为简单功能引入大型框架或服务。
- 完成实现后报告实际验证结果，并区分代码级验证与真实浏览器/PDF 视觉验证。
