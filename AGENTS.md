# AGENTS.md

## 项目目标

本项目是刘纯涛的个人简历与作品集静态网站，用于展示个人定位、技术能力、项目、工作与教育经历以及公开联系方式。

网站只有一个公开版本，不再区分 `private`、`visitor` 或访客预览状态。网站提供“保存为 PDF”能力，并针对 A4 打印和 PDF 导出优化排版。

## 技术原则

- 项目必须能够构建为静态网站，不依赖数据库或运行时后端展示简历。
- 简历数据、业务组件、页面入口和共享类型必须分离。
- 优先采用简单、稳定、易部署和易维护的实现。
- 页面必须兼容桌面端和移动端，并使用语义清晰、可访问性良好的 HTML 结构。
- 动效应克制，并尊重 `prefers-reduced-motion`。
- 不为简单功能引入大型框架或不必要的服务。

## 项目结构

网站业务代码以 `src` 为源码根目录：

```text
src/
├── app/                 # 页面路由、布局和全局样式
├── components/
│   └── resume/          # 简历业务组件
├── data/                # 结构化简历和公开联系方式
├── types/               # 共享 TypeScript 类型
├── lib/                 # 通用工具及外部能力封装
└── db/                  # 数据库相关代码（如未来需要）
```

目录约束：

- 页面入口、布局和全局样式放在 `src/app`。
- 简历相关业务组件放在 `src/components/resume`。
- 简历内容和公开联系方式放在 `src/data`。
- 共享数据类型放在 `src/types`，不要在多个组件中重复定义。
- 跨模块导入优先使用指向 `src` 的 `@/` 路径别名。
- 不要重新创建根目录级别的 `app` 或 `db`。
- `public`、构建配置、测试和基础设施文件继续放在项目根目录。

## 本地运行与构建

```bash
npm run dev
npm run build
npm test
```

不要重新引入私有/访客双构建脚本、`RESUME_PRIVATE` 或联系方式环境变量。若未来需要保护真正的私人内容，应使用服务端认证，而不是静态构建状态或 CSS 隐藏。

## PDF 导出

网站使用 `window.print()` 提供保存为 PDF 能力，并保留专门的打印样式：

- 使用 `@media print` 和 `@page`。
- 默认纸张为 A4，并设置合理页边距。
- 隐藏导航、按钮和其他纯交互元素。
- 避免标题与正文、单条经历或项目卡片被不合理地跨页切断。
- 打印时使用适合白纸阅读的颜色、字号和间距。
- 链接在 PDF 中应保持可识别和可点击。

## 视觉与响应式要求

- 风格应专业、简洁、有个人辨识度，避免过度模板化。
- 首屏清楚呈现姓名、定位、简介和公开联系方式。
- 项目经历突出个人贡献、使用技术和可量化成果。
- 使用项目头像时保持合理裁切，不拉伸、不变形。
- 小屏幕下主要标题、个人介绍和板块引导文案相对居中，保持自然的视觉重心与阅读节奏。

## 开发与验证

普通页面修改后至少执行：

```bash
npm run build
```

验证清单：

1. 网站只存在一个公开版本和一套启动、构建命令。
2. 页面不显示私有/访客切换器，也不存在相关构建逻辑。
3. 公开联系方式可见，点击后能够复制并显示成功提示。
4. “保存为 PDF”按钮可用，打印内容符合 A4 布局。
5. 页面在常见桌面和移动尺寸下可正常阅读。

## 修改约束

- 保持改动聚焦，不覆盖与当前任务无关的用户文件。
- 不提交真实密钥或不应公开的信息。
- 新增依赖前确认其必要性。
- 完成实现后报告实际验证结果，并区分代码级验证与真实浏览器/PDF 视觉验证。
